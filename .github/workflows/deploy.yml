name: Deploy to FTP

on:
  push:
    branches:
      - main
jobs:
  ftp-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repo
        uses: actions/checkout@v4

      - name: Upload to FTP
        uses: SamKirkland/FTP-Deploy-Action@v4.3.4
        with:
          server: webscape.beget.tech
          username: webscape_bsi
          password: hYdH8sKV!txP
          local-dir: ./
          server-dir: webscape.beget.tech/public_html/bsi/wp-content/themes/bsi/
